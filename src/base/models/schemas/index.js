const groupInfo = require('./groupInfo')
const postInfo = require('./postInfo')
const DefaultSchema = require('./defaultSchema')
const postSettings = require('./postSettings')
const deferredSettings = require('./deferredSettings')

module.exports = {
	DefaultSchema,
	groupInfo,
	postInfo,
	postSettings,
	deferredSettings
}
